# Unique name for this setup (image, container, project)
SETUPNAME=lo_dev

# Set your UID and GID in the host environment to modify your file created in the docker environment from host.
UID=2703281
GID=1000